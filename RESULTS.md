# 结果与判读：能量塑形（PBRS）与三阶段甩摆课程

本文记录 2026-09-18 那一轮工作的**实测数字**与判读口径。所有数字都能用仓库里的脚本复现；
每个表都注明样本量、随机种子和置信区间口径，因为本项目历史上吃过"20 回合就下结论"的亏。

---

## 0. 一句话结论

势能塑形（potential-based reward shaping）**可证明不改变最优策略**，实测在平衡区**逐回合零影响**；
把它接到 ±180° 课程上，策略第一次真正学会了甩摆：

| 初始角度带 | P5 基底（纯平衡） | **S2 全角度（塑形）** | 配对 McNemar |
|---|---|---|---|
| (0°, 45°] | 95–100% | **100%** | 无损 |
| (45°, 60°] | 53.3% | **73.3%** | +20.0，p = 0.0005 |
| (90°, 135°] | 0.0% | **31.7%** | +31.7，p < 0.0001 |
| (135°, 180°] | 5.0% | **95.0%** | +90.0，p < 0.0001 |

而且 60–90° 的死区被查清**大部分是导轨长度**，不是策略能力（见第 5 节）。

---

## 1. 口径（务必统一）

| 项 | 取值 |
|---|---|
| 成功判据 | `\|θ\| < 0.12 rad`（6.9°）**且** `\|x\| < 1.5 m` **且** `\|θ̇\| < 1.5 rad/s`，连续保持 100 步（2 s） |
| 回合长度 | 500 步 = 10 s（训练horizon，评估自动跟随 checkpoint 记录值） |
| 甩摆类评估 | **必须** `--terminate-angle 0`（不设角度终止），否则开局即结束——见 README 3.1 |
| 角度带 | `θ₀ ~ U(带内)`，非端点重合：带 i = `(edges[i], edges[i+1]]` |
| 显著性 | Wilson 95% 区间；两策略比较用**配对**精确 McNemar（同种子、同回合） |
| 最小样本 | 每带 60 回合（门槛类判据 200 回合） |

> 两个未配对、各 200 回合的成功率可以因为种子噪声差 3–4 个点。**只有配对检验能作为证据。**

---

## 2. 塑形是什么（以及为什么它不可能破坏平衡）

```
Φ(s) = -c · (E_pend(s) − E*)²          E* = 2·m·g·l = 1.962 J
F(s,s') = γ·Φ(s') − Φ(s)                γ = 0.99
```

`E_pend` = `env.pendulum_energy()`（车体坐标系里杆子的能量：垂下静止 = 0，直立静止 = E*）。

沿一条轨迹求和：

```
Σ_t F_t = γ·Φ(s_T) − Φ(s_0) − (1−γ)·Σ_t Φ(s_t)
```

最后一项是**只依赖状态**的：在固定状态上它对每个动作都取同一个值，因此
`argmax_a Q_shaped(s,a) = argmax_a Q_base(s,a)`（Ng, Harada & Russell 1999）。
所以它既不能让"倒下再翻上来"变成好策略，也无法被刷分（状态空间闭合回路求和恒为 0）——
这是"翻转给一笔大奖"做不到的。

三个性质都有实测断言（`tests/test_sanity.py::test_energy_shaping_is_potential_based`）：

| 性质 | 实测 |
|---|---|
| 每一步都等于势差 | 131 步内最大残差 **7.1e-15** |
| 与动作无关的部分是状态函数 | 同一状态上 5 个不同动作，公共偏移散度 **3.3e-16 J** |
| 平衡区静默 | PD 控制器稳态下最大塑形 **6.2e-05 J**，占同周期奖励的 **0.01%** |
| 批量与标量一致 | 最大奖励差 **7.1e-15**（系数 0 与 1.0 都测） |

> 顺带修掉一个指标缺陷：旧的"能量诊断"用的是 `env.energy()`，**含小车动能**，
> 小车在导轨上高速滑行时会读到 `25×E*`，而杆子几乎没动。现在全部改用 `pendulum_energy()`。
> 写单元测试时还抓出我自己文档里的一处错话（"γ<1 时求和会相消到 γΦ(s_T)−Φ(s_0)"），已改正。

---

## 3. 第一项验收：塑形对平衡**零影响**（最强形式的通过）

同一个检查点（`p5_swing_fixed/best.ckpt`）测两遍：一遍照原样，一遍强制 `--shaping energy --shaping-coef 1.0`。
每格 200 回合、种子 900 起、配对。

| 分布 | 基底 | 塑形版 | 差值 | 不一致对数 | p |
|---|---|---|---|---|---|
| 竖直 | 100.0% | 100.0% | 0 | 0 / 0 | 1.0000 |
| ±15° | 100.0% | 100.0% | 0 | 0 / 0 | 1.0000 |
| 带 (15°,45°]（中心 30°） | 100.0% | 100.0% | 0 | 0 / 0 | 1.0000 |
| ±45° | 98.5% | 98.5% | 0 | 0 / 0 | 1.0000 |
| ±90°（阈值 98.5°） | 61.5% | 61.5% | 0 | 0 / 0 | 1.0000 |
| ±90°（无阈值） | 61.5% | 61.5% | 0 | 0 / 0 | 1.0000 |
| ±180°（无阈值） | 32.0% | 32.0% | 0 | 0 / 0 | 1.0000 |

**7 个分布 × 200 回合，逐回合完全相同**（唯一差别是 `mean_return`：平衡区差 < 0.1，
大角度差 1–3，因为塑形本身计入了回报）。

原因很直白：塑形是 `gap²` 的函数，直立附近 `gap ≈ 0` 且梯度也为 0，所以对稳态平衡**恒等于零**。
这不是"希望不影响"，是可测量的零。

---

## 4. 第二、三项验收：S1 / S2 训练

配方（奖励全程一致，不改课程连续性论证）：

| 阶段 | 起始分布 | warm-start | 迭代 | lr | 熵 | 塑形 | 结果 |
|---|---|---|---|---|---|---|---|
| S1 | `hanging`（纯垂下） | `p5_swing_fixed/best.ckpt` | 400 | 3e-4 | 0.003 | energy c=1.0 | best 在第 259 迭代 |
| S2 | ±180° 整圈 | `s1_shape/best.ckpt` | 300 | **1e-4** | **0.001** | energy c=1.0 | best 在第 164 迭代 |

### 4.1 S1 的能力阶梯（每带 60 回合，种子 900 起）

| 初始角度带 | P5 基底 | S1 | 判读 |
|---|---|---|---|
| (0°, 15°] | 100.0% | 100.0% | 保持 |
| (15°, 30°] | 100.0% | 100.0% | 保持 |
| (30°, 45°] | 95.0% | 96.7% | 保持 |
| (45°, 60°] | 53.3% | 45.0% | 略降（不显著，p = 0.06） |
| (60°, 90°] | 0.0% | 0.0% | 未触及 |
| (90°, 135°] | 0.0% | 0.0% | 未触及 |
| (135°, 180°] | **5.0%** | **36.7%** | **+31.7，p = 0.0001** |

能量诊断（100 回合，±180° 分布）：

| 类别 | n | 撞轨 | 平均 max\|x\| | **平均 max E/E\*** | 平均 min\|θ\| |
|---|---|---|---|---|---|
| 成功 | 46 | 0 | 1.19 m | **1.22** | 0.0° |
| 够到直立没保住 | 20 | 20 | 2.47 m | 2.07 | 1.6° |
| 抽了能没捕获 | 23 | 23 | 2.51 m | **4.16** | 67.0° |
| 从没抽能 | 11 | 11 | 2.52 m | 0.41 | 100.9° |

`max E/E*`（改用 `pendulum_energy`）从基底的 0.9 抬到 **4.16**：
技能确实进了网络，而且**过冲（>E\*）是新的失败模式**——与"塑形抑制过冲"的预期相反，
说明 `c=1.0` 下抽能端被推得太猛，下一轮可以试 `c=0.3–0.5` 或让系数退火。

### 4.2 S2 的能力阶梯（每带 60 回合，种子 900 起）

| 初始角度带 | P5 基底 | S1 | **S2** | 95% CI (S2) |
|---|---|---|---|---|
| (0°, 15°] | 100.0% | 100.0% | **100.0%** | [94.0, 100] |
| (15°, 30°] | 100.0% | 100.0% | **100.0%** | [94.0, 100] |
| (30°, 45°] | 95.0% | 96.7% | **100.0%** | [94.0, 100] |
| (45°, 60°] | 53.3% | 45.0% | **73.3%** | [61.0, 82.9] |
| (60°, 90°] | 0.0% | 0.0% | **0.0%** | [0.0, 6.0] |
| (90°, 135°] | 0.0% | 0.0% | **31.7%** | [21.3, 44.2] |
| (135°, 180°] | 5.0% | 36.7% | **95.0%** | [86.3, 98.3] |
| 合计 | 50.5% | 54.1% | **71.4%** | — |

分段诊断：`(135°,180°]` 单独测 50 回合 = **100%**，而且**没有一次撞轨**（平均 max|x| = 1.56 m）；
`(60°,90°]` 单独测 50 回合 = 2%，其中 **49/50 撞轨**。

### 4.3 S1 的训练崩溃（必须记录）

第 174–264 迭代是稳定高原（val 成功率连续 11 次 = 1.000，|θ| 6–9°，value_loss ≈ 10–40），
**第 269 迭代起突然崩塌**（|θ| → 64°，value_loss 10 → 12 000，KL 0.13 → 0.52），
之后 130 个迭代再没恢复。S2 把 lr 降到 1e-4、熵降到 0.001 后**不再出现这个崩溃**
（value_loss 全程 8–650，val 成功率 0.47–0.72 波动）。

结论：这是**稳定性**问题而非能力问题，低 lr + 低熵是有效对策；
`best.ckpt` 每次都保住了崩溃前的权重，所以资产没有丢失。

---

## 5. 第四项：导轨瓶颈的处理

对 S2 在**同一分布、同一种子**下测两遍导轨长度（每带 60 回合，种子 4000 段）：

| 带 | ±2.4 m（训练用） | 撞轨占比 | ±6.0 m（加长） | 撞轨占比 | p90·\|x\| @6 m |
|---|---|---|---|---|---|
| (0°, 45°] | 100.0% | 0% | 100.0% | 0% | 1.52 m |
| (45°, 60°] | 70.0% | 30% | **100.0%** | 0% | 3.32 m |
| (60°, 75°] | **3.3%** | 96.7% | **81.7%** | 18.3% | 6.03 m |
| (75°, 90°] | **0.0%** | 100% | **1.7%** | 98.3% | 6.21 m |
| (90°, 135°] | 40.0% | 60% | **83.3%** | 16.7% | 6.03 m |
| (135°, 180°] | 98.3% | 1.7% | 100.0% | 0% | 2.41 m |

判读：

1. **60–75° 的死区基本是导轨长度**：3.3% → 81.7%（±2.4 m 下 96.7% 的失败是撞轨，p90|x| 贴着 2.56–2.65 m 上限）。
   而且这一带 46% 的回合**已经够到直立**（min|θ| ≈ 1.8°）只是没保住 → 缺的是"补救所需的行程"，不是抽能技能。
2. **75–90° 是真正的策略缺口**：即使给 6 m 导轨仍然只有 1.7%，小车会跑满 6.21 m 才出界。
   它和相邻两带形成孤立深坑，值得单独设一个**只在这一带起始**的阶段。
3. 加长导轨会改变观测归一化（`_scale(s.x, x_limit)`），所以这一栏是**诊断用的分布外测量**，
   不能当作"策略真实成绩"。它回答的是"是不是导轨的锅"。

---

## 5.5 尝试教"放手再甩起"（S3）——**负面结果，如实记录**

用户的诉求是让策略学会"这个角度救不回来 → 先让它落下去再甩上来"。
**客观条件一律不动**（导轨 ±2.4 m、奖励逐位不变、观测尺度不变），只改**初始状态分布**这一个训练旋钮。

### 5.5.1 先做可行性诊断：教科书版的"放手再甩起"在 2.4 m 下能救多少？

把经典方案（Åström/Furuta 能量律 **+** LQR 捕获，本身就是"先放手、再抽能、最后捕获"）放在
**训练用的 ±2.4 m** 导轨上测（每带 60 回合，种子 5000 段）：

| 初始带 | 成功率 | 撞轨率 | p90·\|x\| |
|---|---|---|---|
| (0°, 45°] | **83.3%** | 16.7% | 2.41 m |
| (45°, 60°] | **0.0%** | 100% | 2.45 m |
| (60°, 75°] | **0.0%** | 100% | 2.53 m |
| (75°, 90°] | **0.0%** | 100% | 2.63 m |
| (90°, 135°] | **0.0%** | 100% | 2.59 m |
| (135°, 180°] | **0.0%** | 100% | 2.56 m |

**45° 以上，经典律一次都没成功，而且每一次失败都是撞轨**（行程 2.45–2.63 m，正好顶死在 ±2.4 m 上）。
在 2.4 m 导轨下，"兜一圈再甩上来"这条路对经典控制器是关着的。

### 5.5.2 学习策略的结局

`s3_band`：warm-start 自 `s2_full_best`，前 15%（45 迭代）hanging 保底、其余 255 迭代
**只在 60–90° 带起始**集中训练（`--init-angle-center 75 --init-angle-limit 15`），
lr 1e-4、熵 0.001、能量塑形 c=1.0 不变。

| 验证点 | 成功率 | 训练回报 | 回合长度 | 策略 std |
|---|---|---|---|---|
| 第 39 迭代（切换前，hanging） | 1.000 | +173.6 | 500 步 | 0.085 |
| 第 49 迭代（刚切到带内） | 0.000 | −111.1 | 33.6 步 | 0.082 |
| 第 169–219 迭代 | 0.000 | −99…−119 | 16.7–21.2 步 | 0.082 |
| 第 294–299 迭代 | **0.000** | −90…−106 | **16.6–17.2 步** | **0.082** |

**255 个迭代里成功率没有离开过 0**（切换后仅第 54–79 迭代出现过 0.031），
回合长度从 33.6 步**退化**到 17 步，策略 std 从第 45 迭代起一直钉在 0.082 表示探索已饱和。
最终阶梯（每带 60 回合）：

| 初始带 | S2 | **S3** | 配对 p |
|---|---|---|---|
| (0°, 45°] | 100.0% | 100.0% | 1.000 |
| (45°, 60°] | 73.3% | 68.3% | 0.375 |
| **(60°, 75°]** | **2.5%**（80 回合探针） | **3.3%** | — |
| **(75°, 90°]** | **0.0%** | **0.0%** | 1.000 |
| (90°, 135°] | 31.7% | 40.0% | 0.458 |
| (135°, 180°] | 95.0% | 100.0% | 0.250 |

**目标带没有任何改善，其它带也没有退化**（所有配对差异都不显著）。

### 5.5.3 判读：为什么这大概率是物理边界，而不是"还没学会"

1. **成功率从 2.5% 提到 3.3% 属于噪声**；而那一带**成功回合的平均峰值行程是 2.27 m，上限 2.4 m
   ——只剩 0.13 m 余量**。成功是"赌到一个很窄的位形"，不是一条可稳定复现的通路。
2. **教科书版的同一条策略在 45° 以上就是 0%**，撞轨率 100%。一条被证明有效的经典控制律都做不到，
   说明限制来自"行程预算 vs 抽能所需相位往返"的物理矛盾。
3. 从 75° 起始，杆子已有 0.75·E\* 的能量，只差 0.25·E\* ≈ **0.49 J** 就能立起来——
   补这点能量要求小车做**相位正确**的往返，而每次往返都要花行程。2.4 m 的预算刚好差那么一点。
4. **(75°, 90°] 更差**：连给 6 m 导轨都只有 1.7%，所以那一带的失败连"导轨"都不能背锅。

**结论**：在 ±2.4 m 导轨下，(60°, 90°] 的"放手再甩起"处在**可行域的边缘**，
天花板是几个百分点；要让这条路变成可靠技能，需要放宽导轨或缩短杆/加大作动力——
都属于改客观条件，已被明确排除。

### 5.5.4 速率课程（S4）与"强制放手"手写策略——同样是负面结果

按"先让杆子接近静止起始，使放手成为唯一可行策略"的思路加了一条**初始角速度课程**
（`--warmup-init-rate-limit`，与 init_mode 共用同一个 `--curriculum-fraction` 窗口）：

| 阶段 | 起始带 | 初始 \|θ̇\| | 迭代 | 成功率 | 回合长度 |
|---|---|---|---|---|---|
| A1 `s4_a1_slow` | 67.5°±7.5 | **0.05**（近乎静止） | 200（跑完） | **0.000** | 17 → 20 步 |
| A2 `s4_a2_wide` | 75°±15 | 0.05 → 0.2 | 54（已停） | 0.000 | 16.6 步 |

`|θ̇₀|` 被压到 1/10 也没有改变结局，说明**问题不在"该不该放手"这个决策的梯度上**。
更糟的是 A1 的最终阶梯显示这次训练**毁掉了已有能力**：

| 初始带 | S2（接手时） | S4-A1（200 迭代后） |
|---|---|---|
| (0°, 45°] | 100% | 96.7–100%（保住） |
| (45°, 60°] | 73.3% | **40.0%** |
| (60°, 90°] | 2.5% / 0% | **0% / 0%**（没涨） |
| (90°, 135°] | 31.7% | **0%** |
| (135°, 180°] | 95.0% | **0%** ← 甩摆能力被抹掉 |

**所以"只在窄带内集中训练"这条路线本身是有害的**：它既没攻下目标带，又把 (90°,180°] 的技能换掉了。

### 5.5.5 决定性的可行性实验：把"放手"写死，看导轨够不够

经典律的问题是它一上来就尝试捕获。把它改成用户描述的顺序——**先强制放手让杆子落下**（前 N 秒零力），
**再交给能量律抽能 + LQR 捕获**——在两条带、五种放手时长下各测 60 回合：

| 起始带 | 放手时长 | 成功率 | 撞轨率 | p90 行程 | **最接近直立 \|θ\|** |
|---|---|---|---|---|---|
| 67.5°±7.5 | 0.0 s | 0.0% | 100% | 2.53 m | 4.8° |
| 67.5°±7.5 | 0.3 / 0.5 / 0.8 / 1.2 s | 0.0% | 100% | 2.48–2.51 m | **0.7–2.6°** |
| 82.5°±7.5 | 0.0 s | 0.0% | 100% | 2.65 m | 36.8° |
| 82.5°±7.5 | 0.3 s | 0.0% | 100% | 2.51 m | **1.4°** |
| 82.5°±7.5 | 0.5 / 0.8 / 1.2 s | 0.0% | 100% | 2.53–2.67 m | 3.5–32.1° |

**这两行数字合起来就是答案**：加上"放手"之后，杆子被甩到**离直立 0.7–2.6°** 的地方
（82.5° 带：36.8° → **1.4°**，放手这一步确实有效）——**但那时的行程已经烧到 2.48–2.67 m，导轨见底，
捕获做不完**。四种独立设置（经典律、学习策略、窄带集中训练、手写放手顺序）全部 0%，且全部撞轨。

**最终结论**：在 ±2.4 m 导轨下，(60°, 90°] 的"放手再甩起"**不是决策问题，是行程预算问题**。
本项目的验收范围应据此调整——这一段作为**诊断分布**，而不是验收分布。

### 5.5.6 增益扫描：50 组配置没有一个能塞进 2.4 m

上一节的"放手 + 抽能"只用了一组增益（`k_energy=1.0, cart_gain=0.3`）。既然经典控制器**有**增益可调
（代码注释里 `cart_gain` 就写着"keeps the rail from being exhausted"），那就该问：
**有没有任何一组增益能把行程压到 2.4 m 以内？** 扫描 5×5 组、每条带 40 回合（放手固定 0.3 s）：

| 带 | k_energy | 成功率 | 撞轨率 | **p50 峰值行程** | p90 峰值行程 | **最接近直立** |
|---|---|---|---|---|---|---|
| 67.5°±7.5 | 0.2 | 0.0% | 100% | 2.44–2.48 m | 2.51–2.53 m | 1.0–1.7° |
| 67.5°±7.5 | 0.5 | 0.0% | 100% | 2.45–2.46 m | 2.49–2.52 m | 0.8–1.0° |
| 67.5°±7.5 | **2.0** | 0.0% | 100% | **2.43 m** | **2.46 m** | **0.3°** |
| 82.5°±7.5 | 0.2 | 0.0% | 100% | 2.50–2.54 m | 2.59–2.65 m | 8.6–23.5° |
| 82.5°±7.5 | **2.0** | 0.0% | 100% | **2.43–2.44 m** | **2.45–2.46 m** | **0.3°** |

（`cart_gain` 从 0.05 到 0.5 几乎没有影响——真正起作用的是 `k_energy`。）

**读法**：抽得越猛，杆子越快到位（最接近直立从 8.6° 改善到 **0.3°**），而且**行程反而略微变短**
（2.50 → 2.43 m）。所以"温柔一点"并不能救它——温柔只会让杆子更晚到位、撞轨时更没准备好。
但即使是最猛的一组，**p50 行程 2.43 m 仍然越过了 2.4 m 的硬边界，成功率 0%**。

结合前面三组数字：

| 来源 | 峰值行程 | 结果 |
|---|---|---|
| 学习策略**成功**的回合（60–90° 带） | **2.27 m** | 成功，余量 0.13 m |
| 能量律最优增益（本次扫描） | **2.43 m** | 全部撞轨 |
| 放手时长扫描（默认增益） | 2.48–2.67 m | 全部撞轨 |
| ±2.4 m 硬边界 | 2.40 m | — |

### 5.5.7 把导轨加到 3.4 m：空间值多少分，以及"为什么不该在窄带上训练"

用户批准改客观条件后做了两件事。**所有测量都把观测尺度 `obs_x_scale` 钉在 2.4 m**
（新增开关，见第 5.5.8），所以这是纯粹的"物理空间"实验，策略看到的特征一字未变。

**(a) 先不训练，直接把 S2 放到更长的导轨上（每带 60 回合）**：

| 初始带 | ±2.4 m | **±3.4 m** | 撞轨率 2.4 → 3.4 | p50·\|x\| @3.4 |
|---|---|---|---|---|
| (0°, 45°] | 100% | 100% | 0 → 0% | 0.73 m |
| (45°, 60°] | 78.3% | **100.0%** | 21.7 → **0%** | 2.00 m |
| **(60°, 75°]** | **1.7%** | **35.0%** | 98.3 → 65% | 3.42 m |
| (75°, 90°] | 0% | 0% | 100 → 100% | 3.54 m |
| (90°, 135°] | 36.7% | 40.0% | 63.3 → 60% | 3.42 m |
| (135°, 180°] | 98.3% | **100.0%** | 1.7 → **0%** | 1.51 m |

**(45°,60°] 与 (135°,180°] 直接满分**——这两段的失败此前**完全**是导轨造成的；
**(60°,75°] 从 1.7% 提到 35%**（+33.3 点），但 p50 行程 3.42 m 说明小车把新导轨**用满了**；
**(75°,90°] 仍是 0%**（p50 3.54 m 同样顶满），与"给 6 m 也只有 1.7%"一致：这条带是真正的缺口。

**(b) 然后在 3.4 m 上训练（warm-start S2，250 迭代，起始带 (45°,90°]∪(135°,180°]）——结果是净损失**：

| 初始带 | S2 @3.4 m | **S5 @3.4 m** | S2 @2.4 m | S5 @2.4 m |
|---|---|---|---|---|
| (0°, 45°] | 100% | 100% | 100% | 100% |
| (45°, 60°] | 100% | 98.3% | 78.3% | 75.0% |
| (60°, 75°] | **35.0%** | 16.7% | 1.7% | 1.7% |
| (75°, 90°] | 0% | 0% | 0% | 0% |
| (90°, 135°] | 40.0% | **0%** | 36.7% | **0%** |
| (135°, 180°] | **100%** | **0%** | 98.3% | **0%** |

把导轨继续加长也救不回来（S5，每带 40 回合）：

| 初始带 | ±2.4 m | ±3.4 m | ±4.5 m | ±6.0 m |
|---|---|---|---|---|
| (45°, 60°] | 75% | 100% | 100% | 100% |
| (60°, 75°] | 0% | 10% | 10% | 10% |
| (75°, 90°] | 0% | 0% | 0% | 0% |
| (90°, 135°] | 0% | 0% | 0% | 0% |
| (135°, 180°] | 0% | **0%** | 0% | 0% |

**所以 S5 不是"需要更长导轨"，而是技能被删掉了**：给它 6 m，(135°,180°] 仍是 0%
（S2 在同带 100%）。这与 S4-A1 是同一个病——**只在一个起始带里训练，会抹掉其它带的能力**，
而且这次即使起始带覆盖了 (135°,180°] 也没保住。结论：3.4 m 导轨**有用**（对未训练的策略
+33 点），但**在那条导轨上继续训练是有害的**；要利用这段空间，必须让全部角度带同时参与训练并作为门控。


**结论（已尽力排除调参可能）**：在 ±2.4 m 导轨上，(60°, 90°] 的"放手再甩起"需要约
**2.4–2.5 m 的行程，正好压在硬边界上**。学习策略的 2–5% 成功率不是"没学会"，
而是**误差容限只剩 0.13 m** 的结果：任何位置偏差直接出界。要让这条路稳定可行，
需要把导轨放宽到约 3 m 以上——即改客观条件。


### 5.5.8 在 3.4 m 上的两次训练：warm-start（S6）与从零（S7/S8）

**S6：S2 warm-start + 全角度 ±180° + 3.4 m（250 迭代）**——比 S5 好，但仍是拆东墙补西墙：

| 初始带 | S2 @3.4 m | **S6 @3.4 m** | S2 @2.4 m | S6 @2.4 m |
|---|---|---|---|---|
| (0°, 45°] | 100% | 100% | 100% | 100% |
| (45°, 60°] | 100% | 98.3% | 78.3% | 56.7% |
| (60°, 75°] | 35.0% | 25.0% | 1.7% | 0% |
| (75°, 90°] | 0% | 0% | 0% | 0% |
| (90°, 135°] | 40.0% | 0% | 36.7% | 0% |
| (135°, 180°] | **100%** | **58.3%** | 98.3% | 36.7% |

**(135°,180°] 的 100% 被换掉了**（门控要求 ≥85%），所以不算成功。

**S7：从零 + 3.4 m（默认 lr 3e-4、无塑形）——第 114 迭代后崩**：val 成功率第 149 迭代还有 0.844，
第 159 迭代掉到 **0.000**（val 回报 −2875、训练回报 −3656、value_loss 1.2e4）。崩前 best 的阶梯全面落后 S2。

**S8：从零 + 3.4 m（lr 1e-4、熵 0.001、500 迭代）——不崩，而且拿到了本项目最好的关键带成绩**。
两次独立测量（每带 60 回合 + 每带 150 回合带 95% 置信区间）：

| 初始带 | S2 @3.4 m | **S8 @3.4 m（150 回合）** | 谁赢 |
|---|---|---|---|
| (30°, 45°] | 100% | 99.3% ± 1.3 | 平 |
| (45°, 60°] | **99.3% ± 1.3** | 43.3% ± 7.9 | **S2** |
| (60°, 75°] | **27.3% ± 7.1** | 6.7% ± 4.0 | **S2** |
| **(75°, 90°]** | **0.0%** | **54.7% ± 8.0** | **S8（史上首次非零）** |
| (90°, 135°] | 41.3% ± 7.9 | **91.3% ± 4.5** | **S8** |
| (135°, 180°] | **98.7% ± 1.8** | 69.3% ± 7.4 | **S2** |
| 加权合计 | 71.0% | **77.4%** | S8 略优 |

三点判读：

1. **(75°,90°] = 54.7%**：这条带在此前**所有**测量里都是 0%（经典控制器、手写放手顺序、
   给到 6 m 导轨、以及全部 warm-start 家族）。**从零训练是第一个攻下它的方法**——所以此前
   "这是策略缺口"的判断要修正为："**这是 warm-start 家族的缺口**"。从 P5 平衡器一路 warm-start
   下来的策略，把"大角度先放手"排除在了解空间之外。
2. **S2 与 S8 能力互补**：没有任何单一策略在每条带都最好。按带取最优的**组合上界是 90.6%**，
   而两者各自只有 71.0% / 77.4%。明确的下一步：**策略蒸馏或集成**，或按初始角度在两者间切换。
3. S8 的弱项是 (45°,60°]（43.3% vs S2 的 99.3%）——从零训练把能力押在了大角度。

### 5.5.10 用两条策略按初始角度切换：三段式规则（**目前最好的可用方案**）

S2 与 S8 能力互补（第 5.5.8 节），所以按**每局开始时可测的初始角度**选一条策略执行到底
（不中途切换，避免打断正在进行的甩摆）。三段式规则：

```
|theta0| <= 72 deg           -> S2     (S2 在 (45,60] 是 99-100%)
72 < |theta0| <= 135 deg     -> S8     (S8 在 (75,90] 55%、(90,135] 85%)
|theta0| > 135 deg           -> S2     (S2 在 (135,180] 是 98-100%)
```

每带 100 回合、所有策略用**同一批种子**（规则只读复位时的状态，不看结果）：

| 初始带 | S2 单独 | S8 单独 | **三段式规则** | oracle |
|---|---|---|---|---|
| (30°, 45°] | 100% | 99% | **100%** | 100% |
| (45°, 60°] | 99% | 45% | **99%** | 99% |
| (60°, 75°] | 21% | 4% | **25%** | 21% |
| (75°, 90°] | 0% | 55% | **55%** | 55% |
| (90°, 135°] | 40% | 85% | **85%** | 85% |
| (135°, 180°] | 98% | 62% | **98%** | 98% |
| **平均** | 59.7% | 58.3% | **77.0%** | 76.3% |
| **按 ±180° 均匀分布加权** | 62.6% | 59.0% | **76.7%** | 75.3% |

（`oracle` = 每带事后取三者最大值，是拿不到的上界；规则与它的差值来自带内混合与采样噪声。）

**单阈值 vs 双区间**：单阈值（`|theta0| <= 72 -> S2`）得 71.5%，缺口全在 (135°,180°]——
阈值把这条带较陡的一半判给了 S8（62%）而 S2 有 98%。改成三段后整体 **+5.5 点**（71.5 → 77.0），
**基本吃满 oracle**。

**唯一剩下的空间是 (60°,75°] 的 25%**：这是唯一"两条策略都不行"的带（S2 21%、S8 4%）。
专门为它训一条被证明有害或无效（S5 毁掉大角度，S9 学不出来），所以目前接受 25%。

### 5.5.11 只训 60–135° 带（S9）——学不出来

按"中间段交给专用策略"的想法，从零训练一条只覆盖 60–135° 的策略
（`--init-angle-center 90 --init-angle-limit 45`，其余照抄能跑完不崩的 S8 配置）：

| 迭代 | best val 成功率 | 训练回报 | 回合长度 |
|---|---|---|---|
| 100 | 0.031 | −250 量级 | 40–60 步 |
| 244 | **0.031**（best） | −224 | 50 步 |
| 274 | 0.031 | −307 | 60–90 步 |

**到第 275 迭代 val 成功率没有超过 3.1%，且从第 244 迭代起完全停滞。**
停掉后用它的 best 量了完整阶梯（每带 60 回合，±3.4 m）：

| 初始带 | S2 | S8 | **S9 best** |
|---|---|---|---|
| (0°, 45°] | 100% | 99% | **21.7%** |
| (45°, 60°] | 99% | 45% | 60.0% |
| (60°, 75°] | 21% | 4% | **0.0%** |
| (75°, 90°] | 0% | 55% | 23.3% |
| (90°, 135°] | 40% | 85% | **3.3%** |
| (135°, 180°] | 98% | 62% | 13.3% |

**它在自己的训练带里也是 0%（(60°,75°]）和 3.3%（(90°,135°]）**，同时把 (0°,45°] 从 100% 毁到 21.7%。
这是**第三个**"窄带从零训练把其它能力一起毁掉"的实例（前两个是 S5、S7），而且这次连目标带都没拿下
——所以"为 (60°,135°] 训一条专用策略"这个假设**被证伪**。

**判读**：60–135° 是**最难学的起始分布**，因为它同时缺两样东西——不像"从垂下出发"那样有明确的
甩摆相位可学（能量从 0 涨到 E\*），也不像"从小角度出发"那样有近直立的局部稳定问题可学。
这一段恰好落在"能量已到 75%、但行程预算最紧"的夹缝里（第 5.5.6 节：需要约 2.43 m 行程）。
这也解释了 S8（全 ±180° 训练）在该带是 0–55%、而它的父代 S2（自平衡器 warm-start）
在 (90°,135°] 有 40%——**两条路线各自只学到了这一段的一半**。

### 5.5.12 顺带修掉的真缺陷

`on_train_epoch_start()` 过去只切换 `train_envs` 的课程，**验证环境从头到尾停在 `warmup_init_mode`**；
而 `best.ckpt` 是按 `val/mean_return` 选的，所以课程已经走到更难分布时，交回来的可能是"在旧分布上最好"
的权重。现在两个向量环境同步跟随课程（`SyncVectorEnv.set_init_mode`），并有回归测试
（`test_curriculum_switches_both_env_sets`）。同时给 `train.py` 补了 `--init-angle-center`
——没有它，`--init-angle-limit 15` 会静默地表示"竖直附近 ±15°"而不是"60–90° 带"，
本节的第一次尝试就是被这个坑掉的。

---

## 6. 结论与下一步

**已验证（正面）**：势能塑形安全（平衡区 7 分布 × 200 回合逐回合零影响）、有效
（(90°,135°] 0 → 31.7%，(135°,180°] 5% → 95%），且在能力阶梯上 S2 的加权成功率从 50.5% 提到 71.4%。

**已验证（负面，四条独立证据）**：在 ±2.4 m 导轨下，(60°,90°] 的"先放手再甩上来"是**行程预算问题**：
经典能量律 0%、手写"强制放手 + 抽能"0%（但能把杆子送到离直立 0.7–2.6°）、窄带集中训练 255 迭代 0%、
速率课程（近乎静止起始）200 迭代 0%。见第 5.5 节。

**不要重复的路线**（已用算力买过结论）：
- 在窄带内**长期集中训练**：既攻不下目标带，还会把 (90°,180°] 的甩摆技能抹掉（S4-A1：(135°,180°] 100% → 0%）；
- 靠**加大探索/降熵**：S3 的 std 从第 45 迭代起钉在 0.082，探索早已饱和；
- 靠**降低初始角速度**让"放手"变得显然：A1 把 |θ̇₀| 压到 0.05（1/10）也没用。

**已验证（组合，第 5.5.10 节）**：S2 + S8 按初始角度三段切换 = **77.0%**（±180° 均匀加权 76.7%），
vs 任一单独策略的 ~59%，接近每带 oracle。这是目前**推荐部署**的方案。

**已验证（导轨，第 5.5.7 节）**：把导轨从 ±2.4 m 加到 ±3.4 m（观测尺度钉住），**不训练**就能让
(45°,60°] 78% → **100%**、(135°,180°] 98% → **100%**、(60°,75°] **1.7% → 35%**；
但**在那条导轨上继续训练是净损失**（(90°,180°] 100% → 0%，给到 6 m 也救不回来）。
需要这段空间时应该**直接部署 S2 + 长导轨**，或让全部角度带同时参与训练并作为门控。

**下一步（按优先级）**：

1. **直接部署三段式切换规则**（第 5.5.10 节，已实测）：S2 与 S8 在 ±3.4 m 上互补，
   按初始角度三段切换得 **77.0%（带均值）/ 76.7%（±180° 均匀加权）**，对比 S2 单独 59.7%、
   S8 单独 58.3%，且**基本吃满每带 oracle（76.3%）**。规则只需 t=0 测一次初始角度：
   `|theta0|<=72 -> S2`；`72<|theta0|<=135 -> S8`；`>135 -> S2`。
   剩下的空间只有 (60°,75°] 的 25%，而专为它训练已被证明有害（S5）或无效（S9）。
   长期可考虑把两条策略**蒸馏**成一条网络，省掉运行时切换。
2. **保护已有资产**：任何"窄带集中训练"都必须以 (90°,180°] 与 (0°,60°] 为门控（S4-A1/S5/S6 的教训），
   并保留 warm-start 的检查点以便回滚。
3. **抽能系数调小**：S1 的 `max E/E*` = 4.16 说明过冲严重，试 `--shaping-coef 0.3–0.5` 或按训练进度退火。
4. **稳定性加固**：给 `CheckpointEveryEpoch` 增加"以 `val/success_rate` 为副监控、连续 N 次退化则停"，
   这样 S1 那种崩溃可以自动止损。
5. **若哪天允许改客观条件**：导轨放大到 4–5 m 是唯一能把 (60°,90°] 变成可靠技能的改动
   （2.4 m 下 p90 行程实测 2.48–2.67 m，就是差那么一点）；届时任务需重新声明。
6. 把本文的阶梯表并入 README 第 10 节；`CURRICULUM_PLAN.pdf` 若要与本文同步需要重新生成（尚未做）。

---

## 7. 复现命令

```powershell
# 自检（含塑形的 6 条断言）
python tests\test_sanity.py

# 能力阶梯（单臂 / 双臂配对）
python scripts\band_report.py --checkpoint outputs\checkpoints\s2_full\best.ckpt `
    --terminate 0 --episodes 60 --bands 0 15 30 45 60 90 135 180 `
    --out outputs\logs\s2_full\band.json

python scripts\band_report.py --checkpoint outputs\checkpoints\s2_full\best.ckpt `
    --base outputs\checkpoints\p5_swing_fixed\best.ckpt `
    --terminate 0 --episodes 60 --bands 0 15 30 45 60 90 135 180

# 训练（奖励全程一致）
python scripts\train.py --model cart --init-mode hanging --terminate-angle 0 `
    --run-name s1_shape --max-epochs 400 --shaping energy --shaping-coef 1.0 `
    --init-from outputs\checkpoints\p5_swing_fixed\best.ckpt --no-render

python scripts\train.py --model cart --init-mode random --init-angle-limit 180 `
    --init-rate-limit 0.5 --terminate-angle 0 --run-name s2_full --max-epochs 300 `
    --learning-rate 1e-4 --entropy-coef 0.001 `
    --shaping energy --shaping-coef 1.0 `
    --init-from outputs\checkpoints\s1_shape\best.ckpt --no-render
```

诊断脚本（`.cache/`，不随仓库分发）：`probe_capture.py`（能量/捕获分类）、
`rail_probe.py`（导轨长度对照）、`compare_arms.py`（三臂对照表）、`acc_paired.py`（验收配对检验）。
