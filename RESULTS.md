# 结果与判读：能量塑形（PBRS）与三阶段甩摆课程

本文记录 2026-09-18 那一轮工作的**实测数字**与判读口径。所有数字都能用仓库里的脚本复现；
每个表都注明样本量、随机种子和置信区间口径，因为本项目历史上吃过"20 回合就下结论"的亏。

---

## 0. 一句话结论

势能塑形（potential-based reward shaping）**可证明不改变最优策略**，实测在平衡区**逐回合零影响**；
把它接到 ±180° 课程上，策略第一次真正学会了甩摆：

| 初始角度带 | P5 基底（纯平衡） | **S2 全角度（塑形）** | 配对 McNemar |
|---|---|---|---|
| (0°, 45°] | 95–100% | **100%** | 无损 |
| (45°, 60°] | 53.3% | **73.3%** | +20.0，p = 0.0005 |
| (90°, 135°] | 0.0% | **31.7%** | +31.7，p < 0.0001 |
| (135°, 180°] | 5.0% | **95.0%** | +90.0，p < 0.0001 |

而且 60–90° 的死区被查清**大部分是导轨长度**，不是策略能力（见第 5 节）。

---

## 1. 口径（务必统一）

| 项 | 取值 |
|---|---|
| 成功判据 | `\|θ\| < 0.12 rad`（6.9°）**且** `\|x\| < 1.5 m` **且** `\|θ̇\| < 1.5 rad/s`，连续保持 100 步（2 s） |
| 回合长度 | 500 步 = 10 s（训练horizon，评估自动跟随 checkpoint 记录值） |
| 甩摆类评估 | **必须** `--terminate-angle 0`（不设角度终止），否则开局即结束——见 README 3.1 |
| 角度带 | `θ₀ ~ U(带内)`，非端点重合：带 i = `(edges[i], edges[i+1]]` |
| 显著性 | Wilson 95% 区间；两策略比较用**配对**精确 McNemar（同种子、同回合） |
| 最小样本 | 每带 60 回合（门槛类判据 200 回合） |

> 两个未配对、各 200 回合的成功率可以因为种子噪声差 3–4 个点。**只有配对检验能作为证据。**

---

## 2. 塑形是什么（以及为什么它不可能破坏平衡）

```
Φ(s) = -c · (E_pend(s) − E*)²          E* = 2·m·g·l = 1.962 J
F(s,s') = γ·Φ(s') − Φ(s)                γ = 0.99
```

`E_pend` = `env.pendulum_energy()`（车体坐标系里杆子的能量：垂下静止 = 0，直立静止 = E*）。

沿一条轨迹求和：

```
Σ_t F_t = γ·Φ(s_T) − Φ(s_0) − (1−γ)·Σ_t Φ(s_t)
```

最后一项是**只依赖状态**的：在固定状态上它对每个动作都取同一个值，因此
`argmax_a Q_shaped(s,a) = argmax_a Q_base(s,a)`（Ng, Harada & Russell 1999）。
所以它既不能让"倒下再翻上来"变成好策略，也无法被刷分（状态空间闭合回路求和恒为 0）——
这是"翻转给一笔大奖"做不到的。

三个性质都有实测断言（`tests/test_sanity.py::test_energy_shaping_is_potential_based`）：

| 性质 | 实测 |
|---|---|
| 每一步都等于势差 | 131 步内最大残差 **7.1e-15** |
| 与动作无关的部分是状态函数 | 同一状态上 5 个不同动作，公共偏移散度 **3.3e-16 J** |
| 平衡区静默 | PD 控制器稳态下最大塑形 **6.2e-05 J**，占同周期奖励的 **0.01%** |
| 批量与标量一致 | 最大奖励差 **7.1e-15**（系数 0 与 1.0 都测） |

> 顺带修掉一个指标缺陷：旧的"能量诊断"用的是 `env.energy()`，**含小车动能**，
> 小车在导轨上高速滑行时会读到 `25×E*`，而杆子几乎没动。现在全部改用 `pendulum_energy()`。
> 写单元测试时还抓出我自己文档里的一处错话（"γ<1 时求和会相消到 γΦ(s_T)−Φ(s_0)"），已改正。

---

## 3. 第一项验收：塑形对平衡**零影响**（最强形式的通过）

同一个检查点（`p5_swing_fixed/best.ckpt`）测两遍：一遍照原样，一遍强制 `--shaping energy --shaping-coef 1.0`。
每格 200 回合、种子 900 起、配对。

| 分布 | 基底 | 塑形版 | 差值 | 不一致对数 | p |
|---|---|---|---|---|---|
| 竖直 | 100.0% | 100.0% | 0 | 0 / 0 | 1.0000 |
| ±15° | 100.0% | 100.0% | 0 | 0 / 0 | 1.0000 |
| 带 (15°,45°]（中心 30°） | 100.0% | 100.0% | 0 | 0 / 0 | 1.0000 |
| ±45° | 98.5% | 98.5% | 0 | 0 / 0 | 1.0000 |
| ±90°（阈值 98.5°） | 61.5% | 61.5% | 0 | 0 / 0 | 1.0000 |
| ±90°（无阈值） | 61.5% | 61.5% | 0 | 0 / 0 | 1.0000 |
| ±180°（无阈值） | 32.0% | 32.0% | 0 | 0 / 0 | 1.0000 |

**7 个分布 × 200 回合，逐回合完全相同**（唯一差别是 `mean_return`：平衡区差 < 0.1，
大角度差 1–3，因为塑形本身计入了回报）。

原因很直白：塑形是 `gap²` 的函数，直立附近 `gap ≈ 0` 且梯度也为 0，所以对稳态平衡**恒等于零**。
这不是"希望不影响"，是可测量的零。

---

## 4. 第二、三项验收：S1 / S2 训练

配方（奖励全程一致，不改课程连续性论证）：

| 阶段 | 起始分布 | warm-start | 迭代 | lr | 熵 | 塑形 | 结果 |
|---|---|---|---|---|---|---|---|
| S1 | `hanging`（纯垂下） | `p5_swing_fixed/best.ckpt` | 400 | 3e-4 | 0.003 | energy c=1.0 | best 在第 259 迭代 |
| S2 | ±180° 整圈 | `s1_shape/best.ckpt` | 300 | **1e-4** | **0.001** | energy c=1.0 | best 在第 164 迭代 |

### 4.1 S1 的能力阶梯（每带 60 回合，种子 900 起）

| 初始角度带 | P5 基底 | S1 | 判读 |
|---|---|---|---|
| (0°, 15°] | 100.0% | 100.0% | 保持 |
| (15°, 30°] | 100.0% | 100.0% | 保持 |
| (30°, 45°] | 95.0% | 96.7% | 保持 |
| (45°, 60°] | 53.3% | 45.0% | 略降（不显著，p = 0.06） |
| (60°, 90°] | 0.0% | 0.0% | 未触及 |
| (90°, 135°] | 0.0% | 0.0% | 未触及 |
| (135°, 180°] | **5.0%** | **36.7%** | **+31.7，p = 0.0001** |

能量诊断（100 回合，±180° 分布）：

| 类别 | n | 撞轨 | 平均 max\|x\| | **平均 max E/E\*** | 平均 min\|θ\| |
|---|---|---|---|---|---|
| 成功 | 46 | 0 | 1.19 m | **1.22** | 0.0° |
| 够到直立没保住 | 20 | 20 | 2.47 m | 2.07 | 1.6° |
| 抽了能没捕获 | 23 | 23 | 2.51 m | **4.16** | 67.0° |
| 从没抽能 | 11 | 11 | 2.52 m | 0.41 | 100.9° |

`max E/E*`（改用 `pendulum_energy`）从基底的 0.9 抬到 **4.16**：
技能确实进了网络，而且**过冲（>E\*）是新的失败模式**——与"塑形抑制过冲"的预期相反，
说明 `c=1.0` 下抽能端被推得太猛，下一轮可以试 `c=0.3–0.5` 或让系数退火。

### 4.2 S2 的能力阶梯（每带 60 回合，种子 900 起）

| 初始角度带 | P5 基底 | S1 | **S2** | 95% CI (S2) |
|---|---|---|---|---|
| (0°, 15°] | 100.0% | 100.0% | **100.0%** | [94.0, 100] |
| (15°, 30°] | 100.0% | 100.0% | **100.0%** | [94.0, 100] |
| (30°, 45°] | 95.0% | 96.7% | **100.0%** | [94.0, 100] |
| (45°, 60°] | 53.3% | 45.0% | **73.3%** | [61.0, 82.9] |
| (60°, 90°] | 0.0% | 0.0% | **0.0%** | [0.0, 6.0] |
| (90°, 135°] | 0.0% | 0.0% | **31.7%** | [21.3, 44.2] |
| (135°, 180°] | 5.0% | 36.7% | **95.0%** | [86.3, 98.3] |
| 合计 | 50.5% | 54.1% | **71.4%** | — |

分段诊断：`(135°,180°]` 单独测 50 回合 = **100%**，而且**没有一次撞轨**（平均 max|x| = 1.56 m）；
`(60°,90°]` 单独测 50 回合 = 2%，其中 **49/50 撞轨**。

### 4.3 S1 的训练崩溃（必须记录）

第 174–264 迭代是稳定高原（val 成功率连续 11 次 = 1.000，|θ| 6–9°，value_loss ≈ 10–40），
**第 269 迭代起突然崩塌**（|θ| → 64°，value_loss 10 → 12 000，KL 0.13 → 0.52），
之后 130 个迭代再没恢复。S2 把 lr 降到 1e-4、熵降到 0.001 后**不再出现这个崩溃**
（value_loss 全程 8–650，val 成功率 0.47–0.72 波动）。

结论：这是**稳定性**问题而非能力问题，低 lr + 低熵是有效对策；
`best.ckpt` 每次都保住了崩溃前的权重，所以资产没有丢失。

---

## 5. 第四项：导轨瓶颈的处理

对 S2 在**同一分布、同一种子**下测两遍导轨长度（每带 60 回合，种子 4000 段）：

| 带 | ±2.4 m（训练用） | 撞轨占比 | ±6.0 m（加长） | 撞轨占比 | p90·\|x\| @6 m |
|---|---|---|---|---|---|
| (0°, 45°] | 100.0% | 0% | 100.0% | 0% | 1.52 m |
| (45°, 60°] | 70.0% | 30% | **100.0%** | 0% | 3.32 m |
| (60°, 75°] | **3.3%** | 96.7% | **81.7%** | 18.3% | 6.03 m |
| (75°, 90°] | **0.0%** | 100% | **1.7%** | 98.3% | 6.21 m |
| (90°, 135°] | 40.0% | 60% | **83.3%** | 16.7% | 6.03 m |
| (135°, 180°] | 98.3% | 1.7% | 100.0% | 0% | 2.41 m |

判读：

1. **60–75° 的死区基本是导轨长度**：3.3% → 81.7%（±2.4 m 下 96.7% 的失败是撞轨，p90|x| 贴着 2.56–2.65 m 上限）。
   而且这一带 46% 的回合**已经够到直立**（min|θ| ≈ 1.8°）只是没保住 → 缺的是"补救所需的行程"，不是抽能技能。
2. **75–90° 是真正的策略缺口**：即使给 6 m 导轨仍然只有 1.7%，小车会跑满 6.21 m 才出界。
   它和相邻两带形成孤立深坑，值得单独设一个**只在这一带起始**的阶段。
3. 加长导轨会改变观测归一化（`_scale(s.x, x_limit)`），所以这一栏是**诊断用的分布外测量**，
   不能当作"策略真实成绩"。它回答的是"是不是导轨的锅"。

---

## 6. 结论与下一步

**已验证**：塑形安全（平衡区逐回合零影响）、有效（(90°,135°] 0→31.7%，(135°,180°] 5→95%）、
且 60–75° 的瓶颈是导轨而非算法。

**下一步（按优先级）**：

1. **75–90° 专项阶段**：从 S2 起，`--init-angle-center 82.5 --init-angle-limit 7.5`，
   其它配置照旧；这是唯一"给足导轨也学不会"的带。
2. **抽能系数调小**：S1 的 `max E/E*` = 4.16 说明过冲严重，试 `--shaping-coef 0.3–0.5` 或按训练进度退火。
3. **稳定性加固**：给 `CheckpointEveryEpoch` 增加"以 `val/success_rate` 为副监控、连续 N 次退化则停"，
   这样 S1 那种崩溃可以自动止损。
4. **导轨决策**（需要在论文/报告里显式声明）：若接受 ±2.4 m 为任务定义，
   则 (60°,90°] 应作为"诊断分布"而非"验收分布"；否则把 `x_limit` 放宽到 4–5 m 并重新声明任务。
5. 把本文三张表并入 README 第 10 节与 `CURRICULUM_PLAN.pdf`。

---

## 7. 复现命令

```powershell
# 自检（含塑形的 6 条断言）
python tests\test_sanity.py

# 能力阶梯（单臂 / 双臂配对）
python scripts\band_report.py --checkpoint outputs\checkpoints\s2_full\best.ckpt `
    --terminate 0 --episodes 60 --bands 0 15 30 45 60 90 135 180 `
    --out outputs\logs\s2_full\band.json

python scripts\band_report.py --checkpoint outputs\checkpoints\s2_full\best.ckpt `
    --base outputs\checkpoints\p5_swing_fixed\best.ckpt `
    --terminate 0 --episodes 60 --bands 0 15 30 45 60 90 135 180

# 训练（奖励全程一致）
python scripts\train.py --model cart --init-mode hanging --terminate-angle 0 `
    --run-name s1_shape --max-epochs 400 --shaping energy --shaping-coef 1.0 `
    --init-from outputs\checkpoints\p5_swing_fixed\best.ckpt --no-render

python scripts\train.py --model cart --init-mode random --init-angle-limit 180 `
    --init-rate-limit 0.5 --terminate-angle 0 --run-name s2_full --max-epochs 300 `
    --learning-rate 1e-4 --entropy-coef 0.001 `
    --shaping energy --shaping-coef 1.0 `
    --init-from outputs\checkpoints\s1_shape\best.ckpt --no-render
```

诊断脚本（`.cache/`，不随仓库分发）：`probe_capture.py`（能量/捕获分类）、
`rail_probe.py`（导轨长度对照）、`compare_arms.py`（三臂对照表）、`acc_paired.py`（验收配对检验）。
